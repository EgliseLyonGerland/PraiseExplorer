---
import Layout from '@/layouts/Layout.astro'
import loadSongs from '@/libs/loadSongs'

const songs = await loadSongs()
const firstSong = songs[0]
---

<meta http-equiv="refresh" content={`0;URL=/${firstSong.id}`} />
<Layout title="Chants - Eglise Lyon Gerland" />
