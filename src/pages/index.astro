---
import Layout from '@/layouts/Layout.astro'
import getSongs from '@/libs/getSongs'

const songs = await getSongs()
const firstSong = songs[0]
---

<Layout title="Chants - Eglise Lyon Gerland" menuOpened />
